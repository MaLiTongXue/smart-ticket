# 改造任务清单

> **目标**：21 天，把这个项目从「别人写的代码」变成「你的项目」。
> **前提认知**：这份清单的核心动作是 **删掉重写**，不是看懂。看懂不值钱，能重写才值钱。

---

## 使用说明（先读这 3 条，不然白做）

### 三条铁律

1. **不许复制粘贴。** 唯一的例外是 `application.yml` 和 `pom.xml` 这类配置文件。
   Java 代码一律手敲，包括 getter/setter（用 IDEA 的 `Alt+Insert` 生成不算抄）。
2. **卡住超过 30 分钟再看参考答案。** 先自己想、自己查报错、自己打日志。
   30 分钟后还没头绪，再翻原代码——这时候看才有价值，印象也深。
3. **每个任务做完，用大白话讲给一个不懂技术的人听。** 讲不顺 = 没真懂，回去补。

### 每天固定动作（雷打不动，和任务并行）

| 项目 | 时长 | 说明 |
|---|---|---|
| 算法题 | 1 小时 | LeetCode Hot 100，从数组/字符串开始，每天 2 题 |
| 八股 | 30 分钟 | 当天的任务对应哪个面试题，就背那个，别贪多 |

### 进度记录

- 每天任务做完，**必须 git commit 一次**，message 写清楚做了什么
- 例：`git commit -m "重写 TicketService：实现分页+新建+状态流转"`

> **为什么强调 commit**：面试时你可以直接打开 GitHub 的提交记录给他看。
> 20 天 20 次提交，真实的迭代轨迹，比任何"精通"两个字都有说服力。

### 进度勾选

做完一个就把 `[ ]` 改成 `[x]`。

---

## 第 0 天：先备份，再动手

- [ ] **D0-1** 在项目根目录初始化 git 并首次提交

```bash
cd C:\Users\w1332\Downloads\smart-ticket
git init
git add .
git commit -m "init: 项目初始版本"
```

- [ ] **D0-2** 确认能回到初始版本

```bash
git log --oneline          # 应该看到一条提交
git tag v0.1-original      # 打个标签，方便以后随时找回原版
```

- [ ] **D0-3** 把 MySQL 和 Redis 正常启动，项目跑起来，8 个接口用 Postman 调通

> ⚠️ **这一步必须做。** 后面你要删文件，万一卡住了，`git checkout v0.1-original -- <文件>` 就能找回。没有这个兜底，你会不敢删。

**预计耗时**：2 小时

---

## 阶段一：吃掉现有代码（Day 1-4）

> 这 4 天的目标不是"看懂"，是**能自己画出完整链路**。画不出来就是没懂。

### Day 1 — 把请求链路跑通并画出来

- [ ] **D1-1** 用 Postman 或 curl 把 8 个接口全部调通，每个都截图存下来
- [ ] **D1-2** 在纸上手画一张图：一个请求从浏览器出发，到返回 JSON，中间经过了哪些环节
  - 提示：Tomcat → 拦截器 → DispatcherServlet → Controller → Service → Mapper → MySQL / Redis
- [ ] **D1-3** 在 `JwtInterceptor.preHandle()` 里加一行 `System.out.println`，观察哪些请求会进来、哪些不会

**完成标准**：合上代码，能对着白纸把链路图默画出来，并说出每一环干什么。

**对应面试题**：一个 HTTP 请求在 Spring Boot 里是怎么被处理的？拦截器和过滤器有什么区别？

**预计耗时**：4 小时

---

### Day 2 — 吃透「统一返回 + 全局异常」

- [ ] **D2-1** 逐行读 `Result.java`，给每个方法写上**你自己理解**的注释（不要抄原来的注释）
- [ ] **D2-2** 读 `GlobalExceptionHandler.java`，回答三个问题写进注释里：
  1. 为什么业务异常返回 500 而不是 400？
  2. `MethodArgumentNotValidException` 是什么时候触发的？
  3. 兜底的 `Exception` 处理里为什么要 `e.printStackTrace()`？这有什么问题？
- [ ] **D2-3** 故意制造 4 种异常，观察返回格式：
  - 传空标题（触发 `@NotBlank`）
  - 查一个不存在的工单 id（触发 `BizException`）
  - 把 MySQL 停掉（触发系统异常）
  - 传一个 JSON 格式错误的请求体

**完成标准**：4 种异常都能返回统一格式，且你能解释每种是怎么被捕获的。

**对应面试题**：你们项目怎么做统一异常处理的？业务异常和系统异常为什么要分开？`@ControllerAdvice` 的原理是什么？

**预计耗时**：4 小时

---

### Day 3 — 吃透 JWT 鉴权链路

- [ ] **D3-1** 读 `JwtUtil.java`，把生成的 token 复制出来，用 <https://jwt.io> 解开看看里面到底是什么
- [ ] **D3-2** 亲手做 4 个实验，记录结果：
  | 实验 | 预期结果 |
  |---|---|
  | 把 token 最后一个字符改掉 | ？ |
  | 不带 token 请求 | ？ |
  | 手动把 `app.jwt.expire` 改成 5000（5 秒）后重新登录，等 6 秒再请求 | ？ |
  | 用 `Bearer` 前缀 vs 不带前缀 | ？ |
- [ ] **D3-3** 回答并写进注释：**JWT 的 payload 是加密的吗？**（这是高频陷阱题）

**完成标准**：能说清 JWT 三段结构，能解释"为什么 payload 里绝对不能放密码"。

**对应面试题**：JWT 和 Session 的区别？JWT 怎么实现注销？token 被盗了怎么办？

**预计耗时**：3 小时

---

### Day 4 — 吃透 Redis 链路

- [ ] **D4-1** 用 `redis-cli` 连上 Redis，先调一次 `/api/ticket/stats`，然后执行：
  ```
  keys *
  get ticket:stats
  ```
  观察存进去的 value 长什么样（是不是带 `@class` 的 JSON）
- [ ] **D4-2** 读 `RedisConfig.java`，回答：**不写这个配置类会怎样？**（实验：注释掉 `RedisConfig`，重启，再看 `get ticket:stats` 的结果）
- [ ] **D4-3** 实验：把 Redis 服务停掉，调 `/api/ticket/stats`，看接口还能不能用
- [ ] **D4-4** 读 `TicketServiceImpl.stats()` 和 `clearStatsCache()`，回答：为什么是**删缓存**而不是**更新缓存**？

**完成标准**：能画出"读缓存 → 命中返回 / 未命中查库 → 写回缓存"的流程图，并解释降级逻辑。

**对应面试题**：RedisTemplate 的序列化方式？缓存穿透/击穿/雪崩？Cache Aside 模式？

**预计耗时**：4 小时

---

## 阶段二：删掉重写（Day 5-11）

> **从今天开始，每天删一个模块，自己重写。** 写完对比原版，找出差异并搞懂为什么。
> 卡住就用 `git show v0.1-original:<文件路径>` 看原版。

### Day 5 — 重写实体类 + Mapper

- [ ] **D5-1** 删掉 `entity/Ticket.java`、`entity/SysUser.java`、`mapper/` 下两个文件
- [ ] **D5-2** 照着 `sql/schema.sql` 自己重写这 4 个文件
- [ ] **D5-3** 回答并写进注释：
  - `@TableName` / `@TableId(type = IdType.AUTO)` / `@TableLogic` 各自解决什么问题？
  - 如果实体类叫 `Ticket`、表叫 `t_ticket`，怎么映射？
  - 为什么 `create_time` 能自动映射到 `createTime`？（这个配置在哪？）

**完成标准**：项目能重新编译通过，启动后能查出 `schema.sql` 里那 3 条测试数据。

**对应面试题**：MyBatis-Plus 的表名/字段名映射规则？逻辑删除的实现原理？

**预计耗时**：3 小时

---

### Day 6 — 重写 TicketService 接口 + 实现（先不写 Redis）

- [ ] **D6-1** 删掉 `service/TicketService.java` 和 `service/impl/TicketServiceImpl.java`
- [ ] **D6-2** 自己定义接口方法，实现这 5 个功能：分页查询、新建、查详情、改状态、删除
- [ ] **D6-3** **暂时不写 Redis 缓存**，就纯查库
- [ ] **D6-4** 打开 SQL 日志，确认每次操作打印的 SQL 是你预期的

**完成标准**：Postman 能调通所有工单接口；能解释 `this.page(...)` 和 `this.count(...)` 是谁提供的方法。

**对应面试题**：`IService` / `ServiceImpl` 提供了哪些方法？为什么 MyBatis-Plus 3.5.9 之后这些类的包名变了？

**预计耗时**：4 小时

---

### Day 7 — 重写 TicketController

- [ ] **D7-1** 删掉 `controller/TicketController.java`，自己重写
- [ ] **D7-2** 用构造器注入，不要用 `@Autowired` 字段注入
- [ ] **D7-3** 回答并写进注释：
  - `@RestController` 和 `@Controller` 有什么区别？
  - `@RequestParam` 和 `@PathVariable` 分别什么时候用？
  - `@RequestBody` 加不加会怎样？

**完成标准**：6 个工单接口全部可用，参数校验生效。

**对应面试题**：为什么推荐构造器注入？Spring 循环依赖怎么解决？

**预计耗时**：3 小时

---

### Day 8 — 重写认证模块

- [ ] **D8-1** 删掉 `dto/LoginRequest.java`、`service/SysUserService.java`、`service/impl/SysUserServiceImpl.java`、`controller/AuthController.java`，自己重写
- [ ] **D8-2** 实现登录逻辑：查用户 → 密码加密比对 → 生成 token → 返回
- [ ] **D8-3** ⭐ **重要作业**：查资料回答下面这个问题，写进注释里：
  > 项目里用的是 MD5 存密码，**这样安全吗？** 应该用什么？
  - 提示：搜「MD5 彩虹表」「BCrypt」「加盐」
  - 这是**你要能主动指出自己项目缺陷**的地方，面试是加分项

**完成标准**：能登录拿到 token；能说出 MD5 存密码的三个问题。

**对应面试题**：密码为什么要加密存储？MD5 和 BCrypt 的区别？什么是彩虹表？

**预计耗时**：4 小时

---

### Day 9 — 重写 JWT 工具类 + 拦截器

- [ ] **D9-1** 删掉 `util/JwtUtil.java`、`interceptor/JwtInterceptor.java`，自己重写
- [ ] **D9-2** 删掉 `config/WebConfig.java` 里的拦截器注册部分，自己写回来
- [ ] **D9-3** 想清楚一个问题：**为什么拦截器里出错要返回 HTTP 200、把 401 放在 body 里？**
  - 提示：如果返回 HTTP 401，前端 axios 会怎么处理？两种做法各有什么优劣？

**完成标准**：登录后能访问接口，不带 token 被拦截，token 被篡改也被拦截。

**对应面试题**：拦截器的 `preHandle` / `postHandle` / `afterCompletion` 执行顺序？多个拦截器的执行顺序？

**预计耗时**：4 小时

---

### Day 10 — 重写统一返回 + 全局异常

- [ ] **D10-1** 删掉 `common/` 下三个文件，自己重写
- [ ] **D10-2** 给 `Result` 加一个新方法：`Result.error(Integer code, String message, T data)`，让它支持带数据的错误返回
- [ ] **D10-3** 给全局异常处理器**新增**一个处理：`HttpMessageNotReadableException`（请求体 JSON 格式错误），返回友好提示而不是 500

**完成标准**：Day 2 的 4 个异常实验重新跑一遍，全部返回统一格式，且 JSON 格式错误有了友好提示。

**对应面试题**：为什么要统一返回格式？`@ExceptionHandler` 的匹配优先级是怎样的？

**预计耗时**：3 小时

---

### Day 11 — 把 Redis 缓存自己加回来

- [ ] **D11-1** 删掉 `config/RedisConfig.java`，自己重写
- [ ] **D11-2** 在 `TicketServiceImpl.stats()` 里自己实现缓存逻辑（读缓存 → 回源 → 写回）
- [ ] **D11-3** 在新建/改状态/删除后**主动删缓存**
- [ ] **D11-4** 加上 try-catch 降级（Redis 挂了不影响接口）
- [ ] **D11-5** ⭐ 加一个日志，打印"缓存命中"还是"回源数据库"，方便你后面压测时观察

**完成标准**：
- 连续调两次 stats，第二次日志显示"命中缓存"
- 新建一个工单后再调 stats，数字正确（说明缓存失效了）
- 停掉 Redis 服务，stats 接口仍然可用

**对应面试题**：缓存和数据库的一致性怎么保证？先删缓存还是先更新数据库？

**预计耗时**：4 小时

---

## 阶段三：加自己的功能（Day 12-16）

> 从这里开始没有参考答案了，**必须自己写**。这部分是你面试时能说"这是我做的"的底气。

### Day 12 — 加工单评论/回复功能（一对多）

- [ ] **D12-1** 设计 `ticket_comment` 表（工单 id、内容、回复人、时间），写进 `sql/`
- [ ] **D12-2** 实体 + Mapper + Service + Controller 全自己写
- [ ] **D12-3** 接口：`POST /api/ticket/{id}/comment`、`GET /api/ticket/{id}/comments`
- [ ] **D12-4** 在工单详情接口里**顺便返回评论列表**（注意别写成 N+1 查询）

**完成标准**：能给工单加评论并查出来。

**对应面试题**：一对多关系怎么设计表？什么是 N+1 查询？怎么避免？

**预计耗时**：5 小时

---

### Day 13 — 加操作日志（AOP 切面）

- [ ] **D13-1** 设计 `sys_oper_log` 表（操作人、操作类型、目标 id、耗时、IP）
- [ ] **D13-2** 自定义一个 `@OperLog` 注解
- [ ] **D13-3** 写一个 `@Aspect` 切面，拦截加了注解的方法，**记录执行耗时并入库**
- [ ] **D13-4** 在 `TicketController` 的方法上使用这个注解

**完成标准**：调接口后，日志表里能看到记录，包含耗时。

**对应面试题**：AOP 的实现原理？JDK 动态代理和 CGLIB 的区别？Spring 事务为什么用 AOP？

**预计耗时**：6 小时

---

### Day 14 — 加数据导出 Excel

- [ ] **D14-1** 引入 EasyExcel（或 Apache POI）依赖
- [ ] **D14-2** 接口：`GET /api/ticket/export` 导出全部工单为 Excel
- [ ] **D14-3** ⭐ 进阶：改成**分页查 + 分批写入**，避免一次性加载全部数据

**完成标准**：浏览器能下载到 Excel 文件，内容正确。

**对应面试题**：导出百万级数据怎么避免 OOM？EasyExcel 比 POI 省内存在哪？

**预计耗时**：5 小时

---

### Day 15 — 加接口限流

- [ ] **D15-1** 实现一个简单限流：用 Redis 的 `INCR` + 过期时间，限制同一 IP 每分钟最多 N 次请求
- [ ] **D15-2** 做成注解 + 拦截器（或 AOP），可以标在任意接口上
- [ ] **D15-3** 用 Postman 的 Runner 快速发 100 次请求，验证超限后返回友好提示

**完成标准**：超过阈值的请求被限流，返回统一格式的提示。

**对应面试题**：常见限流算法有哪些（计数器/滑动窗口/漏桶/令牌桶）？各有什么问题？固定窗口的临界问题是什么？

**预计耗时**：6 小时

---

### Day 16 — 接 Python AI 服务（把这个项目最大的差异化做出来）

- [ ] **D16-1** 启动 Python 服务，先单独把 `/ai/classify` 调通
  ```powershell
  cd ai-service
  pip install -r requirements.txt
  python -m uvicorn main:app --reload --port 8000
  ```
  浏览器打开 <http://localhost:8000/docs> 直接点按钮测试
- [ ] **D16-2** 在 Java 里写一个 `AiService`，用 `RestClient` 调用 Python 服务
- [ ] **D16-3** 用 `@Value("${app.ai.base-url}")` 读配置，把 `app.ai.enabled` 改成 `true` 时生效
- [ ] **D16-4** 在新建工单时调用 AI 自动填 `category` 字段
- [ ] **D16-5** ⭐ **必须做**：加超时（3 秒）+ 失败降级（AI 挂了就留空，不能影响建单）

**完成标准**：新建一条包含「登录不上」的工单，`category` 字段自动变成「账号问题」。

**对应面试题**：调用第三方接口要注意什么？超时、重试、熔断、降级分别是什么？为什么必须设 timeout？

**预计耗时**：6 小时

---

## 阶段四：量化与上线（Day 17-20）

> **这 4 天的产出，直接决定你简历上的数字。**

### Day 17 — 造数据 + 压测 + 优化 + 记录数字

- [ ] **D17-1** 用 SQL 或写个脚本造 **10 万条**工单数据
  ```sql
  -- 思路：用存储过程或 INSERT ... SELECT 自连接快速放大数据量
  ```
- [ ] **D17-2** 用 Postman 或 JMeter 压测 `/api/ticket/stats`，记录响应时间
- [ ] **D17-3** 用 `EXPLAIN` 看分页查询的执行计划，记录 `type` / `rows` / `Extra` 列
- [ ] **D17-4** 针对慢查询加索引，**再测一次，记录优化前后的数字**
- [ ] **D17-5** 把数字填进下面的表格（**这是你简历数字的来源，必须自己测**）

| 场景 | 优化前 | 优化后 | 做了什么 |
|---|---|---|---|
| 分页查询（第 5000 页） | ____ ms | ____ ms | |
| 统计接口（缓存未命中） | ____ ms | ____ ms | |
| 统计接口（缓存命中） | ____ ms | ____ ms | |

**对应面试题**：慢 SQL 怎么定位和优化？`EXPLAIN` 的 `type` 有哪些值？索引失效的场景？

**预计耗时**：6 小时

---

### Day 18 — 部署上线

- [ ] **D18-1** 买一台服务器（阿里云/腾讯云学生机，通常几十块一年）
- [ ] **D18-2** 用 `mvn package` 打出 jar 包
- [ ] **D18-3** 服务器上装 JDK 17/21、MySQL、Redis（或用 Docker）
- [ ] **D18-4** 上传 jar，`nohup java -jar xxx.jar &` 后台运行
- [ ] **D18-5** 配 Nginx 反向代理，能通过公网 IP 访问接口
- [ ] **D18-6** 确认接口文档或一个简单页面能在公网打开

**完成标准**：**拿到一个公网可访问的链接。** 这一步 90% 的应届生做不到。

**对应面试题**：`nohup` 和 `&` 的作用？Nginx 反向代理配置？应用怎么开机自启？

**预计耗时**：8 小时（第一次部署会很折腾，正常）

---

### Day 19 — 写「我这个项目的三个缺陷」

- [ ] **D19-1** 诚实地找出 3 个设计得不好的地方，每个写出：问题 + 影响 + 改进方案
- [ ] **D19-2** 参考方向（自己判断哪些成立，别照抄）：
  - 统计接口用了 4 次 `count` 查询，其实可以一次 `GROUP BY` 搞定
  - MD5 存密码不安全，应该用 BCrypt 加盐
  - AI 调用是同步阻塞的，高并发下会拖慢建单接口，应该改成异步（MQ 或线程池）
  - 没有分布式锁，限流在单机以外会失效
  - 逻辑删除 + 唯一索引 `ticket_no` 会冲突（删了再建同号会失败）
- [ ] **D19-3** 挑一个你最想改的，**真的去改掉它**

**完成标准**：能流利回答"你这个项目哪里设计得不好？"

> ⭐ 这是**面试的杀手题**。答"没什么问题"= 没思考过；能说出三个并给出改进方案 = 真的做过。

**预计耗时**：4 小时

---

### Day 20 — 简历定稿 + 模拟面试

- [ ] **D20-1** 把简历上的项目描述写出来，**每条都是「做了什么 + 用了什么 + 结果（数字）」**
- [ ] **D20-2** 打开 `README.md` 里那张面试题对照表，**逐题自问自答并录音**
- [ ] **D20-3** 听自己的录音，凡是卡壳的、说不清楚的，回去补
- [ ] **D20-4** 把 GitHub 仓库整理干净，README 写清楚（面试官会点进来看）

**完成标准**：12 道面试题能连续讲 30 分钟不卡壳。

**预计耗时**：5 小时

---

## 阶段五：扩展（Day 21+，有余力再做）

- [ ] **E-1** Vue3 + Element Plus 前端：登录页、工单列表、新建工单、详情页
- [ ] **E-2** 接真实 DeepSeek API（申请 Key），做**真模型**分类并统计准确率
- [ ] **E-3** 用向量库（Chroma/Milvus）做历史相似工单检索（RAG 入门）
- [ ] **E-4** 引入 RabbitMQ：建单后异步发通知，把 AI 分类改成异步
- [ ] **E-5** 写 Dockerfile + docker-compose，一条命令启动全部服务
- [ ] **E-6** 加接口文档（Knife4j / SpringDoc）

---

## 完成后你会拥有什么

| 资产 | 为什么值钱 |
|---|---|
| 一个自己重写过的项目 | 面试能扛住追问 5 层 |
| 20+ 次 git 提交记录 | 真实的开发轨迹，不是抄的 |
| 3 张压测数据表 | 简历上**唯一无法造假**的东西 |
| 一个公网链接 | 90% 应届生做不到 |
| 3 个能主动说出的缺陷 | 杀手题有答案 |
| AOP / JWT / Redis / 限流 / 异步 实战 | 八股终于有项目可以挂钩 |

---

## 自查表：做完之后问自己

- [ ] 合上电脑，我能把整个项目的数据流讲清楚吗？
- [ ] 随便指一个文件，我能说出它为什么存在吗？
- [ ] 我的简历上每一个数字，我都能说出是怎么测出来的吗？
- [ ] 如果面试官说「现在给你加个 XX 功能」，我知道从哪个文件开始改吗？
- [ ] 我能说出这个项目三个设计缺陷和改进方案吗？

**任何一项答"不能"，回去补，别投简历。**

---

## 时间不够时的优先级

如果只剩 2 周，按这个顺序砍：

```
必做（砍了就没项目）：
  D0 备份  →  D6 重写Service  →  D7 重写Controller  →  D11 Redis缓存
  →  D16 接AI  →  D17 压测出数字  →  D20 简历+模拟面试

强烈建议：
  D2 异常处理  →  D3 JWT  →  D13 AOP日志  →  D18 部署

可以砍（有余力再做）：
  D12 评论  →  D14 Excel导出  →  D15 限流  →  D19 缺陷分析（这个别砍）
```

> 记住：**一个简单但完全吃透的项目，远胜一个复杂但一知半解的项目。**
> 宁可少做两个功能，也要保证做过的每个都能讲透。
