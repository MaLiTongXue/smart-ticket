# 吃透检查表

> **这不是新计划。** 是给现有计划加的【深度标准】。
> 用法：做到某一天时，翻到当天涉及的文件，逐条自问。答不上来就回去补。

---

# 一、先定义：「吃透」到底是几层

| 层级 | 能力 | 检验方法 | 你现在 |
|---|---|---|---|
| **L0** | 能跑起来 | IDEA Run | ✅ |
| **L1** | 看着代码知道在干嘛 | 给每个文件写注释 | 🟡 部分 |
| **L2** | 合上代码能画出链路 | 白纸默画 | 🟡 |
| **L3** | 删掉能自己写回来 | Day 5-11 重写 | ⬜ |
| **L4** | 能解释每个设计决策 | 被追问 5 层不崩 | ⬜ |
| **L5** | 能指出缺陷并给改进方案 | 回答"哪里设计得不好" | ⬜ |
| **L6** | 能加新功能 / 改架构 | 现场给需求 | ⬜ |

**面试要的是 L4 + L5。** L6 是加分项。

**关键：这不是"读"出来的，是"做"出来的。** 下面给方法。

---

# 二、核心方法：四遍法

```
第一遍  跑通 + 画图        ← 你已完成（Day 1）
        目标：L0 → L2

第二遍  删掉重写            ← Day 5-11
        目标：L2 → L3
        ⚠️ 关键：不许复制，卡住超过 30 分钟才看原代码

第三遍  制造问题            ← 穿插在 Day 12-17
        目标：L3 → L5
        ⚠️ 80% 的人跳过这一遍，所以只能到 L3

第四遍  讲出来 + 写下来      ← Day 19-20
        目标：L5 → L6
```

## ⭐ 第三遍「制造问题」是最被低估的一步

**在破坏中理解。** 具体怎么做：

| 破坏动作 | 你会学到 |
|---|---|
| 关掉 Redis，调统计接口 | 降级逻辑到底有没有用 |
| 把 `RedisConfig` 注释掉，看 `get ticket:stats` | 序列化为什么重要 |
| 把 `pageSize` 传 100000 | 参数校验的作用 |
| 删掉 `MybatisPlusConfig`，看分页 | 分页插件到底干了什么 |
| 造 10 万条数据，不建索引压测 | 索引的真实价值 |
| 把 token 改一个字符 | 签名校验机制 |
| 把 `app.jwt.expire` 改成 5000 | 过期处理 |
| 停掉 MySQL，调接口 | 全局异常兜底 |
| 在 Service 里加 `this.xxx()` 自调用 | 事务失效（Day 13 后） |
| 把连接池 `maximum-pool-size` 改成 1，压测 | 连接池的意义 |

**每破坏一次，你就多一个能讲的面试素材。**

---

# 三、逐文件必答清单

> 做到哪天，就翻到哪天涉及的文件。**能答出来才算过关。**

## pom.xml

- [ ] 每个依赖为什么在？删掉会报什么错？
- [ ] `spring-boot-starter-parent` 的作用是什么？（提示：版本管理）
- [ ] 为什么 MyBatis-Plus 要额外加 `mybatis-plus-jsqlparser`？（提示：3.5.9 拆模块）
- [ ] `<scope>runtime</scope>` 是什么意思？
- [ ] Spring Boot 3.5.16 对应 Spring Framework 哪个版本？（答：6.2.19）

## application.yml

- [ ] 每个配置项的作用？
- [ ] `map-underscore-to-camel-case: true` 不配会怎样？
- [ ] `log-impl: StdOutImpl` 是干嘛的？上线该关吗？
- [ ] `logic-delete-field: deleted` 和实体类上的 `@TableLogic` 什么关系？
- [ ] 数据库密码明文放这里安全吗？生产环境该怎么办？

## common/Result.java　（Day 2）

- [ ] 为什么用泛型 `Result<T>`？不用泛型会怎样？
- [ ] `code` 为什么用 `Integer` 而不是 `int`？
- [ ] 静态方法 `success()` / `error()` 的设计叫什么模式？（工厂方法）
- [ ] 为什么 `data` 可以为 null？

## common/BizException.java　（Day 2）

- [ ] ⭐ 为什么继承 `RuntimeException` 而不是 `Exception`？
      （提示：受检异常必须 try-catch，会污染所有调用链）
- [ ] 为什么只需要一个 `message` 字段？

## common/GlobalExceptionHandler.java　（Day 2）

- [ ] `@RestControllerAdvice` 和 `@ControllerAdvice` 的区别？
- [ ] ⭐ **三个 `@ExceptionHandler` 的匹配优先级是怎样的？**（提示：越具体越优先）
- [ ] 为什么 `BizException` 也返回 `code=500`？合理吗？
- [ ] ⭐ **`e.printStackTrace()` 有什么问题？**（提示：生产日志）
- [ ] `NoResourceFoundException` 被兜底捕获成了 500，应该怎么改？

## config/MybatisPlusConfig.java　（Day 6）

- [ ] ⭐ **不装分页插件会怎样？**（提示：`page()` 会查出全部数据）
- [ ] `PaginationInnerInterceptor` 内部是怎么改写 SQL 的？（加 `LIMIT`）
- [ ] 为什么要指定 `DbType.MYSQL`？

## config/RedisConfig.java　（Day 11）

- [ ] ⭐ **不写这个类会怎样？**（提示：JDK 序列化，redis-cli 看到乱码）
- [ ] `StringRedisSerializer` 和 `GenericJackson2JsonRedisSerializer` 各用在哪？
- [ ] `afterPropertiesSet()` 是干嘛的？

## config/WebConfig.java　（Day 9）

- [ ] `WebMvcConfigurer` 是什么？为什么不用继承 `WebMvcConfigurationSupport`？
- [ ] `addPathPatterns` 和 `excludePathPatterns` 的匹配规则？
- [ ] 为什么跨域要配 `.allowedOriginPatterns("*")` 而不是 `.allowedOrigins("*")`？
      （提示：`allowCredentials(true)` 时不能配 `*`）

## util/JwtUtil.java　（Day 3）

- [ ] ⭐ **JWT 的三段结构分别是什么？**
- [ ] ⭐ **payload 是加密的吗？**（答：只是 Base64，不是加密）
- [ ] `hmacShaKeyFor` 对密钥长度有什么要求？（答：≥ 32 字节）
- [ ] `signWith` 用的是对称加密还是非对称？
- [ ] token 签发后能主动失效吗？（答：不能，需要黑名单/Redis）

## interceptor/JwtInterceptor.java　（Day 3）

- [ ] ⭐ **`preHandle` 返回 `false` 会怎样？**（Controller 不执行，postHandle 不执行）
- [ ] 为什么拦截器只拦 `/api/**`？
- [ ] ⭐ **为什么把 401 放在 body 而不是 HTTP 状态码？**
- [ ] `request.setAttribute("userId", ...)` 目的是什么？

## entity/Ticket.java　（Day 5）

- [ ] ⭐ `@TableName` / `@TableId(type=AUTO)` / `@TableLogic` 各解决什么问题？
- [ ] 为什么不用 Lombok？（提示：本项目的教学选择）
- [ ] `@JsonFormat` 是给谁看的？（Jackson 序列化）
- [ ] `createTime` / `updateTime` 是谁赋值的？（本项目在 Service 里手动赋）

## mapper/TicketMapper.java　（Day 5）

- [ ] ⭐ **为什么是空接口，却能用 `insert()` / `selectById()`？**
      （提示：MyBatis 用 JDK 动态代理在启动时生成了实现类）
- [ ] 如果我要写一个多表关联查询，该怎么做？

## service/TicketService.java + TicketServiceImpl.java　（Day 6、11）

- [ ] ⭐ **为什么要分成接口 + 实现？**（提示：JDK 代理、面向接口编程）
- [ ] `IService` / `ServiceImpl` 提供了哪些方法？
- [ ] ⭐ `this.page(...)` 内部执行了什么 SQL？在哪能看到？
- [ ] ⭐ **统计接口为什么用 Redis 缓存？命中率怎么验证？**
- [ ] ⭐ **为什么是"删缓存"而不是"更新缓存"？**
- [ ] Redis 挂了这个接口会怎样？（答：降级查库）
- [ ] `pageSize > 100` 就改成 10，这个设计合理吗？

## controller/TicketController.java　（Day 7）

- [ ] ⭐ **为什么用构造器注入而不是 `@Autowired` 字段注入？**
- [ ] `@RequestParam` / `@PathVariable` / `@RequestBody` 分别什么时候用？
- [ ] 为什么 Controller 里不写业务逻辑？（提示：定时任务/MQ 也要复用）
- [ ] `@Valid` 加不加有什么区别？

## sql/schema.sql　（Day 17）

- [ ] 每个字段的类型为什么这么选？
- [ ] ⭐ **建了哪些索引？为什么？**
- [ ] `utf8mb4` 和 `utf8` 的区别？（emoji）
- [ ] ⭐ **逻辑删除 + 唯一索引 `ticket_no` 会冲突吗？**
      （删了再建同号工单会失败 —— 这是你要能主动指出的缺陷）

---

# 四、三个检验测试（随时可做）

## 测试 1：白纸测试
**合上电脑，在纸上画出所有类，以及它们的调用关系。**
画不出来 = 还在 L2 以下。

## 测试 2：删除测试
**随机挑一个类/方法，说出"删掉它会发生什么"。**

例：
- 删掉 `RedisConfig` → ？
- 删掉 `MybatisPlusConfig` → ？
- 删掉 `GlobalExceptionHandler` → ？
- 删掉 `JwtInterceptor` 的 WebConfig 注册 → ？
- 删掉 `Result` 改成直接返回对象 → ？

## 测试 3：改动测试
**给一个新需求，说出"要改哪几个文件、按什么顺序改"。**

例：
- "加一个字段：工单支持附件上传" → 改哪几处？
- "统计接口改成按天统计" → 改哪几处？
- "加上'只有 ADMIN 能删除工单'的权限" → 改哪几处？

**能秒答 = L6。**

---

# 五、每天 10 分钟：讲出来 ⭐

**每天结束前，用 10 分钟把今天搞懂的东西，用自己的话讲一遍。**

- 对着手机录音
- 或者打在 txt 里
- 或者讲给室友听

**讲不顺的地方 = 没懂的地方，回去补。**

> 这是**唯一**能检验"我是真懂还是感觉懂"的方法。
> 看得懂 ≠ 讲得出。讲得出 ≠ 经得起追问。

---

# 六、终极武器：写一份自己的项目笔记

**不要只读代码 —— 写一份你自己的文档。**

放在 `docs/我的项目笔记.md`，包含：

| 章节 | 写什么 |
|---|---|
| **架构图** | 你自己画的请求链路、分层图 |
| **每个类的职责** | 一句话说清 |
| **关键设计决策** | 为什么用 JWT 不用 Session？为什么用 Redis 缓存统计？ |
| **踩过的坑** | 环境坑、编码坑、MyBatis-Plus 拆模块、CGLIB 代理 |
| **已知缺陷** | 至少 3 个 + 改进方案 |
| **面试自问自答** | 把上面第三节的问题，写下你的答案 |

**这份文档就是你的面试讲稿。** 面试前看它，比看任何八股文都有用 —— 因为**每一句都是你自己的**。

---

# 七、⚠️ 吃透最大的敌人

```
"我看懂了"        ← 最危险的状态
```

**看代码时的"懂"，是一种错觉。** 因为你顺着作者的思路走，每一步都合理，所以你感觉懂了。

**真正的检验只有四种输出：**

| 输出 | 检验 |
|---|---|
| ✍️ **写出来** | 删掉重写，能跑通 |
| 🎨 **画出来** | 白纸画链路图 |
| 🗣️ **讲出来** | 讲给别人听，对方能听懂 |
| ❓ **被追问** | 连问 5 层不崩 |

**四种都能做到 = 真吃透。**

---

# 八、现在的应用

你现在的进度：

```
Day 1 ✅  跑通 + 观察调用栈（L0 → L2 进行中）
Day 2 ⬜  全局异常处理
Day 3 ⬜  JWT 鉴权
...
```

**今天做 Day 2 + Day 3 时，就对照第三节里 `Result` / `BizException` / `GlobalExceptionHandler` / `JwtUtil` / `JwtInterceptor` 那几块的问题。**

**每答不上来一条，就是一条你今天该补的。**
