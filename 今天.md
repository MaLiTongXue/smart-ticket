# 今天

**2026-09-15 星期二**　|　**进度：Day 1 ✅ → 今天做 Day 2 + Day 3**

> ⚠️ **只看这一页。** `TASKS.md` 和 `每日计划.md` 是字典，卡住了才查。
> 计划：**9/25 前完成全部 20 个任务**，每天 2 个。

---

# 第 0 步：收尾 Day 1（3 分钟）

**① 删调试代码**
打开 `src/main/java/com/smartticket/service/impl/TicketServiceImpl.java`，删掉这一行：

```java
new Exception("看看谁调了我").printStackTrace();
```

**② 提交**

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
git add .
git commit -m "Day 1: 跑通 8 个接口，梳理请求链路，观察调用栈"
```

---

# 任务 A：Day 2 —— 吃透「统一返回 + 全局异常」

**预计 2.5 小时**

## A1　读代码（20 分钟）

打开 `src/main/java/com/smartticket/common/` 下三个文件，逐行读：

| 文件 | 看什么 |
|---|---|
| `Result.java` | 泛型 `Result<T>` 是怎么统一返回格式的 |
| `BizException.java` | 为什么继承 `RuntimeException` 而不是 `Exception`？ |
| `GlobalExceptionHandler.java` | `@RestControllerAdvice` 和 `@ExceptionHandler` 怎么配合 |

**给每个文件写上你自己的注释**（不是抄原来的）。

## A2　做 4 个实验（40 分钟）

先把项目 Run 起来。依次制造 4 种异常，**记录每次的返回内容**：

| # | 怎么做 | 预期 |
|---|---|---|
| 1 | POST `/api/ticket`，body 里 `title` 传空字符串 | `code:400`（`@NotBlank` 触发） |
| 2 | GET `/api/ticket/99999`（不存在的 id） | `code:500` 提示"工单不存在" |
| 3 | 管理员 PowerShell 执行 `Stop-Service MySQL80`，再调接口 | `code:500` + 数据库错误 |
| 4 | POST `/api/ticket`，body 传一段坏的 JSON（比如 `{"title":`） | ？自己看 |

> 实验 3 做完记得 `Start-Service MySQL80`。
> **建议用一个表格把 4 次结果记下来** —— 这是你 Day 10 重写这个类时的对照表。

## A3　想 3 个问题（30 分钟）

打开兜底方法：

```java
@ExceptionHandler(Exception.class)
public Result<Void> handleException(Exception e) {
    e.printStackTrace();
    return Result.error("服务器开小差了：" + e.getMessage());
}
```

**问题：这段代码有 3 个毛病。分别是什么？**

（先自己想 15 分钟，想不出来再看提示）

<details>
<summary>提示</summary>

1. `e.printStackTrace()` 打印到**控制台** —— 项目部署到服务器后，谁去看控制台？
2. 把 `e.getMessage()`（可能包含 **SQL 语句、表名、数据库连接串**）**原样返回给前端**，有什么安全风险？
3. 昨天你发现的：`NoResourceFoundException`（本该是 **404**）也被兜成了 **500**。为什么？

</details>

## ✅ A 的完成标准

- [ ] 三个文件都加上了**你自己写的**注释
- [ ] 4 种异常的返回结果都记录下来了
- [ ] 3 个毛病能用自己的话说出来

---

# 任务 B：Day 3 —— 吃透 JWT 鉴权链路

**预计 2.5 小时**

## B1　把 token 拆开看（20 分钟）

1. 跑一次登录，拿到 token：

```powershell
powershell -ExecutionPolicy Bypass -File tools\api-test.ps1
```

2. 从输出里复制 token，粘到 <https://jwt.io>
3. **看它的三段结构**：头部、载荷、签名

## B2　做 4 个实验（60 分钟）

| # | 实验 | 记录结果 |
|---|---|---|
| 1 | 把 token 最后**一个字符**改掉，再请求 | ？ |
| 2 | 完全不带头 `Authorization` | ？ |
| 3 | 把 `application.yml` 里 `app.jwt.expire` 改成 `5000`（5秒），重启，登录后**等 6 秒**再请求 | ？ |
| 4 | 请求头写成 `Authorization: eyJ...`（**不带 `Bearer ` 前缀**） | ？ |

> 实验 3 做完把 `expire` 改回 `604800000`。

## B3　回答一个陷阱题（30 分钟）

**JWT 的 payload 是加密的吗？** 别人拿到你的 token，能不能看到里面的内容？能不能改？

（提示：你用 jwt.io 解开 token 的时候，**需要密码吗？**）

## B4　想一个问题（20 分钟）

打开 `JwtInterceptor.java`，看这段：

```java
response.setStatus(HttpServletResponse.SC_OK);          // ← 返回 HTTP 200
response.getWriter().write("{\"code\":401,...}");         // 把 401 放在 body 里
```

**为什么不用 `response.setStatus(401)`？** 两种做法各有什么优劣？

（提示：前端用 axios 时，HTTP 401 会触发什么？）

## ✅ B 的完成标准

- [ ] 能说清 JWT 三段分别是什么
- [ ] 能回答"payload 是不是加密的"
- [ ] 4 个实验的结果都记录下来了
- [ ] 能解释"为什么把 401 放在 body 而不是 HTTP 状态码"

---

# 收尾（5 分钟）

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
git add .
git commit -m "Day 2-3: 吃透全局异常处理与 JWT 鉴权链路"
git log --oneline
```

---

# 今天的结束标志

**`git log --oneline` 里有 3 条提交。**

看到就收工。

---

# 如果做不完

**优先保 A（Day 2），B 可以顺延到明天。**

绝不允许的是：
- ❌ 为了赶进度，抄着过（**每个"完成标准"必须真的达到**）
- ❌ 熬到凌晨 2 点（**每天上限 8 小时**）
- ❌ 连续两天不做（**这条永不取消**）

如果今天只完成了 A，那也算成功 —— **明天接着做 B，不算断。**

---

> 📌 做完告诉我一声，我把明天的（Day 4 + Day 5）放进这一页。
