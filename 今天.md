# 今天

**2026-09-17 星期四**　|　**进度：Day 1-5 ✅　→ 今天做 Day 6**

> ⚠️ **今天要删项目里最核心的类。** 安全网已建好（`git tag v0.1-original`），删了能救回来。

---

# Day 6：删掉重写 TicketService + TicketServiceImpl

**目标：把 Service 层删掉，自己从零写回来。**

**预计 2.5 小时。这是项目里最核心的类，也是面试问得最多的。**

---

# 任务 A：先读懂要删的东西（25 分钟）

## ① 打开 `TicketService.java`（接口）

看它声明了几个方法，每个方法的**返回类型**和**参数**：

```java
public interface TicketService extends IService<Ticket> {

    IPage<Ticket> pageQuery(Integer pageNum, Integer pageSize, Integer status, String keyword);

    Ticket createTicket(Ticket ticket);

    Ticket getDetail(Long id);

    void updateStatus(Long id, Integer status);

    void deleteTicket(Long id);

    Map<String, Object> stats();
}
```

**对照 `TicketController.java`，看每个接口对应 Service 里的哪个方法。**

## ② 打开 `TicketServiceImpl.java`（实现）

**它分成两大块：**

| 块 | 方法 | 有没有 Redis |
|---|---|---|
| **纯数据库操作** | `pageQuery` / `createTicket` / `getDetail` / `updateStatus` / `deleteTicket` | ❌ 没有 |
| **带缓存** | `stats` / `clearStatsCache` | ✅ 有 |

**今天只重写【纯数据库操作】那一块，`stats` 和 `clearStatsCache` 先留着不动**（Day 11 再重写缓存）。

## ③ 记录：这几个"看不懂"的东西

读的时候留意这些，写的时候会用到：

| 你看到的 | 记下来 |
|---|---|
| `this.page(...)` | 谁提供的方法？ |
| `this.count(...)` | 谁提供的方法？ |
| `LambdaQueryWrapper<Ticket>` | 这是干什么的？ |
| `wrapper.eq(...)` / `.like(...)` / `.orderByDesc(...)` | 这些方法在拼什么？ |
| `new Page<>(pageNum, pageSize)` | 这个对象是什么？ |
| `throw new BizException(...)` | 为什么要抛这个？ |

---

# 任务 B：删掉重写（80 分钟）⭐ 核心

## 🚫 铁律

> **不许复制粘贴。**
> 可以看 `TicketController.java`（它告诉你 Service 需要哪些方法），
> **但代码必须自己敲。**

## ① 删掉这两个文件

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
Remove-Item src\main\java\com\smartticket\service\TicketService.java
Remove-Item src\main\java\com\smartticket\service\impl\TicketServiceImpl.java
```

**删完 IDEA 会飘红（Controller 找不到 Service）—— 正常。**

## ② 自己写回来

**建议顺序：先写接口，再写实现。**

### 第 1 步：写 `TicketService.java`（接口）

```java
package com.smartticket.service;

import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.spring.service.IService;
import com.smartticket.entity.Ticket;

import java.util.Map;

public interface TicketService extends IService<Ticket> {

    // ← 在这里声明 6 个方法
    // 提示：从 TicketController 里看它调了哪些方法、传什么参数
}
```

### 第 2 步：写 `TicketServiceImpl.java`（实现）

```java
package com.smartticket.service.impl;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.spring.service.impl.ServiceImpl;
import com.smartticket.common.BizException;
import com.smartticket.entity.Ticket;
import com.smartticket.mapper.TicketMapper;
import com.smartticket.service.TicketService;
import org.springframework.stereotype.Service;
import org.springframework.util.StringUtils;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.concurrent.ThreadLocalRandom;

@Service
public class TicketServiceImpl extends ServiceImpl<TicketMapper, Ticket> implements TicketService {

    // ========== 6 个方法，自己写 ==========

    // ① pageQuery：分页查询
    //    提示：用 LambdaQueryWrapper 拼条件 + this.page(...)
    //    要点：pageNum/pageSize 要做参数兜底（非法值给默认值）
    //          status 不为 null 才加 eq 条件
    //          keyword 有值才加 like 条件（标题 or 客户名）
    //          按 createTime 倒序

    // ② createTicket：新建
    //    要点：生成 ticketNo（"TK" + yyyyMMdd + 3位随机数）
    //          补默认值（status=0, priority=2, createTime, updateTime, deleted=0）
    //          调 this.save(ticket)

    // ③ getDetail：查详情
    //    要点：this.getById(id)，查不到就 throw new BizException("工单不存在，id = " + id)

    // ④ updateStatus：改状态
    //    要点：先校验 status 只能是 0/1/2/3
    //          先调 getDetail(id) 确认工单存在
    //          改 status + updateTime，调 this.updateById(ticket)

    // ⑤ deleteTicket：删除
    //    要点：先调 getDetail(id) 确认存在
    //          调 this.removeById(id)

    // ⑥ stats：统计（今天先照抄原版，Day 11 再重写）
    //    要点：先查 Redis，没有就查库并写回，全程 try-catch 降级
}
```

## ③ 卡住了怎么办

| 卡了多久 | 怎么办 |
|---|---|
| < 30 分钟 | **自己扛** —— 看 `TicketController.java`、看 `吃透检查表.md` |
| > 30 分钟 | 看原版：`git show v0.1-original:src/main/java/com/smartticket/service/impl/TicketServiceImpl.java` |

**⚠️ 看完立刻关掉，别对着抄。**

## ④ 验证

```powershell
# ① Ctrl + F9 编译
# ② Run 项目
# ③ 跑测试
powershell -ExecutionPolicy Bypass -File tools\api-test.ps1
```

**预期：`通过 8 项，失败 0 项`**

### ⭐ 还要额外看 SQL 日志

**在 IDEA 控制台里，对着每一轮请求数 SQL：**

| 接口 | 应该有几条 SQL | 你看几条 |
|---|---|---|
| `GET /api/ticket/page` | `COUNT(*)` + `SELECT ... LIMIT` = **2 条** | |
| `GET /api/ticket/{id}` | `SELECT ... WHERE id = ?` = **1 条** | |
| `POST /api/ticket` | `INSERT` = **1 条** | |
| `PUT /api/ticket/{id}/status` | `SELECT`（查存在）+ `UPDATE` = **2 条** | |
| `DELETE /api/ticket/{id}` | `SELECT` + `UPDATE deleted=1` = **2 条** | |

**数量对不上说明你的逻辑写错了。**

---

# 任务 C：回答 3 个问题（20 分钟）

**把答案写成注释，写进 `TicketServiceImpl.java`。**

### 问题 1

**`this.page(...)` 和 `this.count(...)` 是谁提供的方法？**

提示：`Esc` 都不用按，`Ctrl+B` 跳进去看。

答：________________________________

### 问题 2 ⭐ 面试高频

**`LambdaQueryWrapper` 是什么？为什么用它，而不是直接写 SQL 字符串？**

提示：它的 `eq` / `like` / `orderByDesc` 在拼什么？

答：________________________________

### 问题 3

**`throw new BizException(...)` 之后，异常是怎么变成 HTTP 响应的？**

提示：你 Day 2 学过 `GlobalExceptionHandler`。

答：________________________________

---

# 完成标准

- [ ] 两个文件**自己敲**出来了（不是复制的）
- [ ] `Ctrl+F9` 编译通过
- [ ] `api-test.ps1` 通过 8 项
- [ ] **SQL 日志的数量和预期一致**
- [ ] 3 个问题答出来了

---

# 提交

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
git add .
git commit -m "Day 6: 删掉重写 TicketService 和 TicketServiceImpl"
git log --oneline
```

**看到 7 条提交 = Day 6 完成。**

---

# 做完之后

**把 3 个问题的答案 + SQL 日志的数量发我。**

然后我把 **Day 7（重写 TicketController）** 写进这一页。

> 💡 **今天的难度比 Day 5 高** —— 因为 Service 里有业务逻辑（参数兜底、工单编号生成、状态校验），不是纯粹的字段映射。
> **但也正因为这样，这一块是面试问得最多的。**
