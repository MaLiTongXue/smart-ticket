# 今天

**2026-09-16 星期三**　|　**进度：Day 1 ✅　Day 2 ✅　Day 3 ✅　→ 今天做 Day 4**

> ⚠️ **只看这一页。** 卡住了才去翻 `TASKS.md` 或 `吃透检查表.md`。

---

# Day 4：吃透 Redis 链路

**目标一句话：能说清"统计接口为什么用 Redis、缓存怎么保证不脏、Redis 挂了会怎样"。**

**预计 2 小时。**

> 💡 命令里的 `D:\CODE\redis-cli.exe` 是你机器上 Redis 的路径，直接用。

---

## 前置：把项目 Run 起来

IDEA 里 Run `SmartTicketApplication`，等到 `Tomcat started on port 8080`。
**然后别关它。**

---

## 任务 A：看缓存里到底存了什么（20 分钟）

### ① 先清空 Redis，确保是干净的

```powershell
& "D:\CODE\redis-cli.exe" FLUSHDB
```

返回 `OK` 就行。

### ② 调一次统计接口，让它写缓存

```powershell
Add-Type -AssemblyName System.Net.Http
$c = New-Object System.Net.Http.HttpClient

# 登录
$body = New-Object System.Net.Http.StringContent('{"username":"admin","password":"123456"}', [System.Text.Encoding]::UTF8, 'application/json')
$login = [System.Text.Encoding]::UTF8.GetString($c.PostAsync('http://localhost:8080/api/auth/login', $body).Result.Content.ReadAsByteArrayAsync().Result)
$tk = ($login | ConvertFrom-Json).data.token

# 调统计
$req = New-Object System.Net.Http.HttpRequestMessage([System.Net.Http.HttpMethod]::Get, 'http://localhost:8080/api/ticket/stats')
$req.Headers.Add('Authorization', "Bearer $tk")
$r = $c.SendAsync($req).Result
"统计接口返回: " + [System.Text.Encoding]::UTF8.GetString($r.Content.ReadAsByteArrayAsync().Result)
$c.Dispose()
```

### ③ 去 Redis 里看

```powershell
"=== 有哪些 key ==="
& "D:\CODE\redis-cli.exe" KEYS *

"=== 这个 key 里存的是什么 ==="
& "D:\CODE\redis-cli.exe" GET ticket:stats

"=== 它还有多久过期（秒）==="
& "D:\CODE\redis-cli.exe" TTL ticket:stats
```

### 记录

| 问题 | 你的答案 |
|---|---|
| key 的名字是什么？ | |
| value 长什么样？（是乱码还是可读的 JSON？） | |
| TTL 返回多少？（缓存活多久） | |

> 💡 如果 value 是**可读的 JSON**，说明 `RedisConfig` 里的序列化配置生效了。
> 如果是**一堆 `\xac\xed` 开头的乱码**，说明用的是 JDK 默认序列化。

---

## 任务 B：再调一次，看缓存有没有生效（10 分钟）

```powershell
$c = New-Object System.Net.Http.HttpClient
$body = New-Object System.Net.Http.StringContent('{"username":"admin","password":"123456"}', [System.Text.Encoding]::UTF8, 'application/json')
$login = [System.Text.Encoding]::UTF8.GetString($c.PostAsync('http://localhost:8080/api/auth/login', $body).Result.Content.ReadAsByteArrayAsync().Result)
$tk = ($login | ConvertFrom-Json).data.token
$req = New-Object System.Net.Http.HttpRequestMessage([System.Net.Http.HttpMethod]::Get, 'http://localhost:8080/api/ticket/stats')
$req.Headers.Add('Authorization', "Bearer $tk")
$null = $c.SendAsync($req).Result
$c.Dispose()
"已再调一次，去 IDEA 控制台找这行："
"   统计数据命中缓存"
```

### 记录

| 问题 | 你的答案 |
|---|---|
| 这次控制台有没有打印"统计数据命中缓存"？ | |
| 两次请求之间，SQL 日志里有没有出现 `SELECT COUNT(*)`？（第二次应该没有） | |

---

## 任务 C：把 Redis 停掉，看降级（15 分钟）

**管理员 PowerShell：**

```powershell
Stop-Service Redis
```

**回到普通窗口，再调一次统计接口**（用任务 B 那段命令）。

### 记录

| 问题 | 你的答案 |
|---|---|
| 接口还能用吗？（能 / 不能） | |
| 返回的内容和之前一样吗？ | |
| IDEA 控制台有没有 WARN 日志？内容是什么？ | |

**测完启动回来：**

```powershell
Start-Service Redis
```

> 🎯 **这一步验证的是"降级"** —— 缓存挂了，服务不能跟着挂。

---

## 任务 D：读代码 + 回答 3 个问题（40 分钟）

打开 `src/main/java/com/smartticket/service/impl/TicketServiceImpl.java`，**只看这两个方法**：

- `stats()` —— 读缓存的逻辑
- `clearStatsCache()` —— 删缓存的逻辑

**然后回答（先自己想，想不出来再看代码）：**

### 问题 1 ⭐ 为什么是"删缓存"而不是"更新缓存"？

```
方案 A（现在的做法）：
   数据变了 → 删掉缓存 → 下次读的时候重新查库并写回

方案 B（另一种做法）：
   数据变了 → 直接算出新的值 → 更新缓存
```

**方案 A 好在哪？**（提示：如果算新值的逻辑很复杂，或者有多个地方会改数据）

答：________________________________

### 问题 2 ⭐ 缓存的 TTL 设了多少？为什么是这个值？

看代码里的 `STATS_CACHE_SECONDS`。

答：________________________________

**追问**：如果 TTL 设成 1 小时会怎样？设成 1 秒又会怎样？

答：________________________________

### 问题 3 如果 Redis 挂了，缓存还写不进去吗？代码怎么处理的？

看 `stats()` 里那两段 `try-catch`。

答：________________________________

---

## 任务 E：三个面试必问的概念（20 分钟）

**不用背标准答案，用你项目的代码来理解。**

### ① 缓存穿透

> 查询一个**数据库里根本不存在**的数据，缓存里也没有 → 每次都打到数据库。

**你项目里会中招吗？**

想一想：如果有人拿 `GET /api/ticket/99999999` 疯狂请求，会怎样？

答：________________________________

### ② 缓存击穿

> **某一个热点 key 过期**的瞬间，大量请求同时打到数据库。

**你项目里会中招吗？**（提示：`ticket:stats` 是不是热点？）

答：________________________________

### ③ 缓存雪崩

> **大量 key 同时过期**，或者 Redis 整个挂掉，所有请求都打到数据库。

**你项目里会中招吗？**

答：________________________________

> 💡 这三个问题的答案都是"**会中招**"—— 因为项目里没做任何防护。
> **这就是缺陷 7、8、9。** 但你不用现在改，只要**能说出来**就够了。

---

# 完成标准

- [ ] 看到了 `ticket:stats` 缓存的真实内容和 TTL
- [ ] 验证了"第二次调接口命中缓存"
- [ ] 验证了"Redis 停掉接口仍可用"（降级生效）
- [ ] 3 个代码问题都能答出来
- [ ] 能说出缓存穿透/击穿/雪崩的区别，以及项目里会中招的地方

---

# 提交

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
git add .
git commit -m "Day 4: 吃透 Redis 缓存链路，验证序列化与降级"
git log --oneline
```

---

# 做完之后

**把任务 A~E 的记录发我**，我批改。

然后我把 **Day 5（重写 Entity + Mapper）** 写进这一页 —— **从 Day 5 开始就要删代码了，记得安全网已经建好了（`git tag v0.1-original`）。**
