# 今天

**2026-09-16 星期三**　|　**进度：Day 1 ✅ Day 2 ✅ Day 3 ✅ Day 4 ✅　→ 今天做 Day 5**

> ⚠️ **从今天开始要删代码了。** 第 0 步的安全网演练**必须先做**，做完你才敢下手。

---

# Day 5：删掉重写 Entity + Mapper

**目标：把 4 个文件删掉，自己从零写回来。**

**预计 2 小时。**

> 这是 Day 5-11「删掉重写」阶段的第一天。之后每天删一个模块。

---

# ⭐ 第 0 步：安全网演练（10 分钟，必须做）

**在做任何删除之前，先确认你能救回来。**

## ① 先看看安全网在不在

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
git tag
git log --oneline
```

应该看到标签 `v0.1-original` 和 5 条提交。

## ② 故意删掉一个文件

```powershell
Remove-Item src\main\java\com\smartticket\entity\Ticket.java
```

## ③ 看 git 的反应

```powershell
git status
```

**你应该看到：**

```
deleted:    src/main/java/com/smartticket/entity/Ticket.java
```

## ④ ⭐ 救回来

```powershell
git checkout -- src/main/java/com/smartticket/entity/Ticket.java
```

## ⑤ 确认恢复成功

```powershell
git status
```

**应该显示"工作区干净"（没有任何改动）。**

## ✅ 演练成功

**现在你知道了：删掉的文件随时能救回来。** 所以 Day 5-11 可以放心删。

> 💡 **如果想看原版文件的内容**（不是恢复，只是查看）：
> ```powershell
> git show v0.1-original:src/main/java/com/smartticket/entity/Ticket.java
> ```
> **但记住规则：卡住超过 30 分钟才能看。**

---

# 任务 A：先读懂要删的东西（20 分钟）

**删之前先知道要删什么、为什么。**

## ① 看数据库表结构（这是"数据来源"）

打开 `sql/schema.sql`，重点看 `ticket` 表：

```sql
CREATE TABLE ticket (
    id              BIGINT       NOT NULL AUTO_INCREMENT,
    ticket_no       VARCHAR(32)  NOT NULL,
    title           VARCHAR(200) NOT NULL,
    content         TEXT,
    category        VARCHAR(50),
    priority        TINYINT      NOT NULL DEFAULT 2,
    status          TINYINT      NOT NULL DEFAULT 0,
    customer_name   VARCHAR(50),
    customer_phone  VARCHAR(20),
    ai_suggestion   TEXT,
    create_time     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    deleted         TINYINT      NOT NULL DEFAULT 0,
    ...
);
```

**记一下字段名规律：数据库用下划线（`ticket_no`），Java 用驼峰（`ticketNo`）。**

## ② 读要删的 4 个文件

| 文件 | 看什么 |
|---|---|
| `entity/Ticket.java` | 字段怎么和表字段对应？用了哪些注解？ |
| `entity/SysUser.java` | 同上 |
| `mapper/TicketMapper.java` | 为什么只有几行？ |
| `mapper/SysUserMapper.java` | 同上 |

## ③ 记录：这 4 个文件里用了哪些注解？

把你看到的注解列出来：

```
Ticket.java 用了：________________________________
SysUser.java 用了：________________________________
Mapper 用了：________________________________
```

---

# 任务 B：删掉重写（60 分钟）⭐ 核心

## 🚫 铁律

> **不许复制粘贴。**
> 可以看 `sql/schema.sql` 的字段列表（那是数据来源），
> **但 Java 代码必须自己敲。**

## ① 删掉这 4 个文件

```powershell
Remove-Item src\main\java\com\smartticket\entity\Ticket.java
Remove-Item src\main\java\com\smartticket\entity\SysUser.java
Remove-Item src\main\java\com\smartticket\mapper\TicketMapper.java
Remove-Item src\main\java\com\smartticket\mapper\SysUserMapper.java
```

**删完 IDEA 会满屏飘红 —— 这是正常的，别慌。**

## ② 自己写回来

**建议顺序：**
1. 先写 `Ticket.java`（字段多，先搞定它）
2. 再写 `SysUser.java`（字段少，很快）
3. 最后写两个 Mapper（最简单，各 3 行）

**写 `Ticket.java` 时，对着 `sql/schema.sql` 的字段一个一个写。**

## ③ 卡住了怎么办

| 卡了多久 | 怎么办 |
|---|---|
| < 30 分钟 | **自己扛** —— 查 IDEA 的报错、看 `吃透检查表.md` |
| > 30 分钟 | 看原版：`git show v0.1-original:src/main/java/com/smartticket/entity/Ticket.java` |

**⚠️ 看的时候只看"缺什么"，看完立刻关掉，别对着抄。**

## ④ 验证写对了

```powershell
# 编译
mvn -q clean compile
```

或者直接在 IDEA 里 **Build → Build Project**（`Ctrl+F9`）。

**编译通过后，Run 起来，跑一次接口测试：**

```powershell
powershell -ExecutionPolicy Bypass -File tools\api-test.ps1
```

**应该显示「通过 8 项」。**

> 🎯 **这就是"写对了"的证明** —— 如果实体类字段映射错了，SQL 会报错，接口必然失败。

---

# 任务 C：回答 3 个问题（20 分钟）

**写完之后回答，把答案写成注释写进代码里。**

### 问题 1

**`@TableName` / `@TableId` / `@TableLogic` 各自解决什么问题？**

提示：
- 不写 `@TableName` 会怎样？（提示：类名 `Ticket`，表名 `ticket`）
- `@TableId(type = IdType.AUTO)` 里的 `AUTO` 是什么意思？
- `@TableLogic` 你 Day 4 已经懂了

答：________________________________

### 问题 2

**如果实体类叫 `Ticket`，但数据库表叫 `t_ticket`，该怎么映射？**

答：________________________________

### 问题 3

**为什么数据库的 `create_time` 能自动映射到 Java 的 `createTime`？这个配置写在哪儿？**

提示：去 `application.yml` 里找。

答：________________________________

---

# 完成标准

- [ ] 安全网演练做过了，能删能救
- [ ] 4 个文件**自己敲**出来了（不是复制的）
- [ ] IDEA 编译通过，没有飘红
- [ ] `api-test.ps1` 跑出「通过 8 项」
- [ ] 3 个问题答出来了

---

# 提交

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
git add .
git commit -m "Day 5: 删掉重写 Entity 和 Mapper"
git log --oneline
```

**看到 6 条提交 = Day 5 完成。**

---

# 做完之后

**把这 3 个问题的答案发我，我批改。**

然后我把 **Day 6（重写 Service 层）** 写进这一页 —— 那才是重头戏，`TicketServiceImpl` 是项目里最核心的类。
