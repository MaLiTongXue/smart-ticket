# 今天

**2026-09-15 星期二 21:00**　|　**进度：Day 1 ✅　Day 2 ✅　→ 现在做 Day 3（最小版）**

> **Day 2 已打卡。** 下面这个是"加餐"，做完算超额，不做也不算断。
> 时间预估：**60-90 分钟**。做完就睡。

---

# Day 3 最小版：搞懂 JWT 到底是什么

**目标一句话：能回答"JWT 的 payload 是加密的吗？"**

---

## 第 1 步：把 token 拆开看（15 分钟）

### ① 先拿一个真 token

```powershell
Add-Type -AssemblyName System.Net.Http
$c = New-Object System.Net.Http.HttpClient
$body = New-Object System.Net.Http.StringContent('{"username":"admin","password":"123456"}', [System.Text.Encoding]::UTF8, 'application/json')
$r = $c.PostAsync('http://localhost:8080/api/auth/login', $body).Result
$json = [System.Text.Encoding]::UTF8.GetString($r.Content.ReadAsByteArrayAsync().Result)
$tk = ($json | ConvertFrom-Json).data.token
"复制下面这行去 jwt.io："
""
$tk
```

### ② 粘到 <https://jwt.io>

**把上面输出的那串东西，粘到 jwt.io 左侧的 "Encoded" 框里。**

### ③ 记录你看到的

| 问题 | 你的答案 |
|---|---|
| token 被几个 `.` 分成了几段？ | |
| 第 1 段（红色）解开是什么？ | |
| 第 2 段（紫色）里有哪些字段？ | |
| 第 3 段（蓝色）是什么？ | |
| **解开它需要密码吗？** | |

---

## 第 2 步：4 个实验（30 分钟）

**项目要先 Run 起来。**

整段粘贴到 PowerShell：

```powershell
Add-Type -AssemblyName System.Net.Http

function Try-Request($label, $authHeader) {
  $c = New-Object System.Net.Http.HttpClient
  $c.Timeout = [TimeSpan]::FromSeconds(10)
  $req = New-Object System.Net.Http.HttpRequestMessage([System.Net.Http.HttpMethod]::Get, 'http://localhost:8080/api/ticket/page')
  if ($authHeader) { $req.Headers.Add('Authorization', $authHeader) }
  $r = $c.SendAsync($req).Result
  $txt = [System.Text.Encoding]::UTF8.GetString($r.Content.ReadAsByteArrayAsync().Result)
  "===== $label ====="
  if ($txt.Length -gt 130) { $txt = $txt.Substring(0,130) + ' ...' }
  "  $txt"
  ""
  $c.Dispose()
}

# 登录取 token
$c = New-Object System.Net.Http.HttpClient
$body = New-Object System.Net.Http.StringContent('{"username":"admin","password":"123456"}', [System.Text.Encoding]::UTF8, 'application/json')
$login = [System.Text.Encoding]::UTF8.GetString($c.PostAsync('http://localhost:8080/api/auth/login', $body).Result.Content.ReadAsByteArrayAsync().Result)
$tk = ($login | ConvertFrom-Json).data.token
$c.Dispose()

Try-Request '实验 0: 正常带 token（对照）'      "Bearer $tk"
Try-Request '实验 1: 把 token 最后一位改掉'      ("Bearer " + $tk.Substring(0, $tk.Length-1) + "X")
Try-Request '实验 2: 完全不带 Authorization 头'  $null
Try-Request '实验 3: 去掉 Bearer 前缀'           $tk
```

把 4 个结果记到表里：

| # | 实验 | 返回的 code | 返回的 message |
|---|---|---|---|
| 0 | 正常带 token | | |
| 1 | 末位改一个字符 | | |
| 2 | 不带 Authorization | | |
| 3 | 去掉 Bearer 前缀 | | |

---

## 第 3 步：回答 2 个问题（15 分钟）

### 问题 A ⭐ 高频陷阱题

**JWT 的 payload 是加密的吗？**

提示：你用 jwt.io 解开它的时候，**需要输入密码吗？**

答：________________________________

### 问题 B

打开 `JwtInterceptor.java`，看拦截失败时的代码：

```java
response.setStatus(HttpServletResponse.SC_OK);          // HTTP 200
response.getWriter().write("{\"code\":401,...}");         // 401 放在 body 里
```

**为什么不用 `response.setStatus(401)`？**

（提示：前端用 axios 时，HTTP 401 会触发什么？）

答：________________________________

---

# 完成标准

- [ ] 能说出 token 被分成几段、每段是什么
- [ ] 能回答"payload 是加密的吗"（**必须答对**）
- [ ] 4 个实验的结果都记录下来了
- [ ] 实验 3（去掉 Bearer 前缀）的结果你可能觉得意外 —— 记下来

---

# 提交

```powershell
cd C:\Users\w1332\Downloads\smart-ticket
git add .
git commit -m "Day 3: 拆解 JWT 结构，完成鉴权 4 个实验"
git log --oneline
```

---

# 今天结束的标准

**`git log` 里有 4 条提交。**

看到就收工，**别熬夜**。剩下的（改过期时间那个实验）明天补。

---

> 📌 做完把 4 个实验的结果 + 两道题的答案发我，我批改。
> 做完不想发了也行 —— 明天直接开始 Day 4。
